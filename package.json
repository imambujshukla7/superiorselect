{
  "name": "superiorselect",
  "version": "1.0.0",
  "description": "SuperiorSelect - MERN Stack Application",
  "main": "README.md",
  "scripts": {
    "start": "cd client && npm run build && cd ../server && npm start",
    "dev": "cd client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mern", "superiorselect"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=12.0.0"
  },
  "private": true,
  "workspaces": [
    "client",
    "server"
  ]
}
